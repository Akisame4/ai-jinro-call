# AI人狼 通話ツール 修正指示メモ

現行実装に対する修正3点。着手前に現行コードの該当箇所（映像トラック管理・レイアウト管理・RTDB構造）を確認し、影響範囲を報告してから実装すること。

---

## 修正1: 画面共有とカメラを別枠で表示する

### 現状の問題
画面共有を開始すると、自分のカメラ映像が消えて共有画面に置き換わる。

### 期待する動作
- 画面共有中も**カメラ映像は残り、共有画面は別のタイルとして追加表示**される
- 自分の画面でも他の参加者の画面でも同様
- 共有停止時は共有タイルだけが消え、カメラはそのまま

### 実装方針
- 現状はおそらく `RTCRtpSender.replaceTrack()` でカメラトラックを差し替えている。これをやめ、**画面共有用の映像トラックを別途追加（addTrack / addTransceiver）**する
- 受信側がカメラと共有を区別できるよう、**ストリームの種別（camera / screen）をRTDBに登録**する（例: `members/{peerId}/streams/{streamId}: "camera" | "screen"`）。MediaStream ID か transceiver の mid で紐付ける
- トラック追加でSDP再交渉が発生する。現行の「後から入った側がofferを出す」ルールは初回接続専用なので、**再交渉時のglare対策（Perfect Negotiation パターン等）を導入**すること
- 共有停止（ブラウザの「共有を停止」ボタン押下を含む＝`track.onended`）で、送信停止・RTDBの種別情報削除・受信側のタイル削除まで行う

### 帯域
メッシュなので共有映像は5人分送信になる。共有映像の上限は **1本あたり 800kbps〜1Mbps程度**を目安に `maxBitrate` で制限する（文字が読める最低ラインで調整）。

---

## 修正2: レイアウト同期（申請・許可制）

### 期待する動作
1. 参加者Aが「レイアウト同期」を申請する
2. 他の参加者に申請通知が出て、**各自が個別に許可/拒否**する
3. 許可した人の画面は、**Aの画面構成（各タイルの位置・サイズ・重なり順・表示/非表示）と同じ**になる
4. Aがタイルを動かすと、許可した人の画面も**リアルタイムで追従**する

### 実装方針
- Aのレイアウトを RTDB に書き込み、追従者が購読する（例: `layouts/{peerId}`）
- **座標は画面サイズに依存しない相対値（0〜1）で保存**する。各自のウィンドウサイズ・解像度が違うため、px保存だとズレる
- タイルの識別は**表示位置ではなく peerId + ストリーム種別**で行う（修正1の共有タイルも同期対象に含める）
- ドラッグ中の書き込みは**間引く（100ms程度のスロットル）**。Sparkプランの書き込み量を抑えるため
- 申請・許可の状態も RTDB で管理（例: `layoutSync/{leaderId}/followers/{peerId}: "pending" | "accepted" | "rejected"`）

### 仕様として決めておくこと（未決・要確認）
- 追従中に自分でタイルを動かしたら → **案: 追従中は自分の操作をロックし、「同期解除」ボタンで抜ける**
- 追従者がいつでも解除できるか → **案: できる。解除時はその時点のレイアウトのまま自由操作に戻る**
- 申請者Aが退室・切断したら → **案: 自動で同期解除、最後のレイアウトを維持**
- 同時に複数人が申請したら → **案: 申請者は同時に1人まで。先着優先で、他の申請は拒否表示**

---

## 修正3: 各メンバー視点の一括録画（YouTube編集素材用）

### 目的
YouTube動画の撮影をこのツールでまとめて行う。通話画面に共有していないメンバーの画面（プレイ視点）も、**編集素材として各自分を録画しておく**。通話画面に表示する必要はない。

### 期待する動作
1. 各メンバーが事前に「録画する画面」を選んでおく（getDisplayMedia）。選んだ画面は**他の参加者には送信しない**
2. ホストが「一斉録画開始」を押すと、**全員の端末で同時に録画が始まる**
3. ホストが「一斉録画停止」を押すと全員停止し、各自の端末にファイルが保存される
4. 収録後、各自がファイルをホストに送る（受け渡しはツール外。Googleドライブ等）

### 実装方針
- **録画は各自のブラウザでローカル実行**（MediaRecorder）。映像を送信しないので通話の帯域に影響せず、通信品質に左右されない高画質素材が残る
- 開始/停止の合図は RTDB 経由（例: `recording/{ROOM_ID}: { state: "recording" | "stopped", startedAt }`）
- 各メンバーの録画状態（待機中/録画中/保存完了/エラー）を RTDB に書き、**ホスト画面で全員の状態を一覧表示**する（録り逃し防止）
- 長時間収録でメモリがあふれないよう、**MediaRecorder を timeslice 付きで動かし、File System Access API（showSaveFilePicker）でディスクに逐次書き込む**。録画データをメモリに溜め込む実装は不可（1時間で数GBになる）
- ファイル名に「表示名_録画開始時刻」を入れる

### 編集時の同期対策
- 各ファイルの録画開始時刻（ミリ秒）を記録し、ファイル名またはログに残す
- 加えて、開始直後に**全員の画面に同期用の合図（数フレームのフラッシュ等）**を出す機能を検討。編集で波形・映像を揃える目印にする

### 出力形式・画質
- **MP4（H.264 + AAC）で出力**: `video/mp4;codecs=avc1,mp4a.40.2` を指定。`MediaRecorder.isTypeSupported()` で確認し、非対応環境のみ WebM（vp8,opus）にフォールバックして画面に警告を出す
- 映像ビットレートは `videoBitsPerSecond: 6000000`（6Mbps・1080p想定）、音声 128kbps
- 容量目安: **1時間あたり約2.8GB**。録画開始前に各自の保存先の空き容量を表示し、3時間分（約9GB）未満なら警告する

### 収録時間
- ディスクへ逐次書き込みのため、**録画時間の上限は保存先の空き容量のみで決まる**（アプリ側で上限は設けない）
- 長時間収録中にPCがスリープすると録画が止まるため、録画中は画面に「スリープ設定を切ること」の注意を表示する

### 要検証
- ChromeのMP4出力ファイルをDaVinci Resolveで読み込み、シーク・音ズレに問題がないか確認する（問題があればffmpegでの再格納手順を用意）
- ゲーム音の録音: 画面全体の共有時にシステム音声を含められるか（Windows + Chrome）を確認

### 対象外
- カメラ映像・マイク音声のローカル録画は行わない（通話画面の録画で賄う）

---

## 共通

- RTDB構造の変更点は仕様書に反映すること
- CLAUDE.md に修正内容の要約を追記すること
- テスト用の仮実装には `// [TEST]` を付けること
